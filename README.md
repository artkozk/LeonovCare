# LeonovCare Site

Статический маркетинговый сайт карьерного менторства в IT с интеграцией Telegram-бота, тарифов, калькулятора автооткликов и отзывов через API.

## Что внутри

- `index.html` — главная страница с офферами, FAQ, отзывами из API.
- `zero-offer.html`, `interview-prep.html`, `grade-salary.html`, `autoapply.html` — сервисные лендинги.
- `directions.html` — хаб направлений.
- `java.html`, `golang.html`, `frontend.html`, `python.html`, `php.html`, `qa.html`, `ml.html`, `analytics.html`, `devops.html`, `mobile.html`, `data-engineering.html` — страницы треков.
- `interview-helper.html` — страница программы помощи во время собеседований.
- `config.js` — центральный источник контента и бизнес-настроек.
- `script.js` — единая клиентская логика рендера/интерактива.
- `styles.css` — дизайн-система и стили компонентов.
- `offer.html` — публичная оферта.
- `sitemap.xml`, `robots.txt` — SEO-технические файлы.

## Ключевая архитектура

- Сайт **данно-ориентированный**: тексты, тарифы, направления, график зарплат и API-настройки лежат в `config.js`.
- Все рендеры строятся в `script.js` по `data-*` и id-контейнерам, без фреймворка.
- Telegram-переходы формируются с deep-link payload для маршрутизации внутри бота.
- Отзывы грузятся через API IT Mentors (без фото-блока и без ручного обновления контента).

## Быстрый запуск локально

Любой статический сервер:

```powershell
cd C:\prog\Comercial\LeonovCareSite
python -m http.server 8080
```

Открыть: `http://localhost:8080`.

## Настройка отзывов API

В `config.js`:

```js
const reviews = {
  apiBaseUrl: "https://api.it-mentors.ru/v1",
  telegramId: "ВАШ_NUMERIC_TG_ID",
  perPage: 0
};
```

- `telegramId` — обязателен (числовой ID ментора).
- `perPage: 0` — запрашивать все отзывы.

## Полная спецификация

Подробный документ для воспроизведения проекта другой нейросетью:

- [`SITE_REPRO_SPEC.md`](SITE_REPRO_SPEC.md)
- [`BOT_SALES_DESCRIPTION_RU.md`](BOT_SALES_DESCRIPTION_RU.md) — актуальное продающее описание Telegram-бота для потенциального покупателя (без описания функций сайта и без описания парсинга контактов HH).

## Адаптив: защита от поломок

Базовый responsive-hardened слой вынесен в `styles.css` и покрывает все страницы, которые используют общий шаблон:

- Включен защитный режим от горизонтального переполнения (`overflow-x: clip`) и добавлены `min-width: 0` для ключевых flex/grid-контейнеров.
- Для текстов добавлен `overflow-wrap: anywhere`, чтобы длинные фразы и ссылки не ломали сетку на узких экранах.
- Кнопки переведены в безопасный режим (`max-width: 100%`, перенос текста внутри кнопки).
- Усилен брейкпоинт `<=760px`: более компактный header, контролируемая типографика, стек кнопок в колонку.
- Добавлен брейкпоинт `<=480px`: компактные отступы карточек/CTA, уменьшенные кнопки и безопасные размеры модальных/навигационных элементов.

Рекомендуемая ручная проверка перед релизом:

1. `320x568`
2. `375x667`
3. `390x844`
4. `768x1024`
5. `1280x800`

Проверяем, что нет горизонтального скролла, обрезанных кнопок/заголовков, наложений меню и «ломаных» карточек.

## Деплой на прод leonovcare.ru

Nginx-конфиг домена указывает на директорию:

- `/main/LeonovCare/LeonovCare`

Базовый деплой через git:

```bash
ssh root@85.198.82.221
cd /main/LeonovCare/LeonovCare
git fetch --all
git pull --ff-only origin main
nginx -t && systemctl reload nginx
```

Проверка:

1. `curl -I https://leonovcare.ru/`
2. Открыть главную + `interview-prep.html` с мобильным viewport в DevTools.

## Обновление 2026-04-17 (без удаления старых блоков)

Ниже зафиксировано, как сайт работает сейчас после правок продукта.
Старые пункты выше сохранены специально как исторический контекст.

### 1) Коммуникационные ссылки и контакты внизу сайта

Что работает сейчас:

1. Основной контакт ментора изменен на `@LeonovCare` (`https://t.me/LeonovCare`).
2. Добавлен YouTube-контакт `@leonov_care` (`https://www.youtube.com/@leonov_care`).
3. В общий footer теперь автоматически добавляется блок контактов на всех страницах:
   - ментор;
   - Telegram-бот;
   - Telegram-канал;
   - YouTube.

Почему сделано именно так:

1. Контакты добавляются через единый JS-инжект в `script.js`, чтобы не дублировать одинаковую разметку вручную по всем HTML-файлам.
2. Это снижает риск рассинхронизации контактов между страницами.

### 2) Автоотклики: новая ценовая модель и удаление калькулятора

Что работает сейчас:

1. Удален интерактивный калькулятор автооткликов (`slider`) с `autoapply.html`.
2. Введен единый месячный тариф: `500 ₽ / месяц` за `до 200 откликов в день`.
3. Добавлен пробный период: `1 день бесплатно` с доступом к Interview Helper и автооткликам.
4. В `config.js` блок `autoapply` обновлен:
   - `rateRules` описывает пробный период;
   - `packs` очищен;
   - `subscriptions` содержит месячный тариф.

Почему сделано именно так:

1. Убрали лишнюю вариативность тарифов и ручной калькулятор, чтобы путь к запуску был короче и понятнее.
2. Пробный день закрывает проверку сервиса без upfront-оплаты и снижает барьер входа.

### 3) Разовые услуги по 5 000 ₽

Что работает сейчас:

1. Добавлена отдельная разовая услуга `Составление резюме` за `5 000 ₽` в тарифах `interview-prep`.
2. В тарифах `grade-salary` заменен разовый `Аудит грейда` на `Составление резюме` за `5 000 ₽`.
3. Разовый `mock-собес` остается отдельной услугой за `5 000 ₽`.

Почему сделано именно так:

1. Запрошены два явных one-time сценария с одинаковой ценой: резюме и mock.
2. Карточки добавлены в data-layer (`config.js`), чтобы изменение сразу применялось в рендере без правок шаблонов.

### 4) Удаление пользовательской подписи `Interview Prep`

Что работает сейчас:

1. Публичные текстовые вхождения `Interview Prep` заменены на русскую формулировку `подготовка к интервью` в контентных блоках/roadmap.

Почему сделано именно так:

1. Убран непонятный пользователю англоязычный маркер из пользовательского интерфейса.
2. Сохранена сущность маршрута/slug (`interview-prep`) как технический идентификатор страницы.

### 5) График зарплат по направлениям (блок со скрина)

Что работает сейчас:

1. Расширен список направлений в `languageSalaryChart.data`:
   - добавлены `DevOps / SRE`, `C# / .NET`, `Backend`.
2. Обновлен заголовок графика и пояснение: теперь он позиционируется как график направлений разработки.

Почему сделано именно так:

1. По запросу “дополнить список на фото 2” увеличили набор направлений в самом графике.
2. Изменения внесены в `config.js`, чтобы сортировка и интерактив в `script.js` продолжили работать без отдельной логики.

## Актуализация 2026-04-17 (возврат старого дизайна + фиксация финальной логики)

Ниже добавлено уточнение поверх предыдущих блоков без удаления истории.

### 1) Возврат визуальной базы

Что работает сейчас:

1. Сайт возвращен на старую дизайн-базу (без экспериментального редизайна).
2. Все продуктовые изменения накатываются на прежнюю верстку, чтобы не ломать привычный интерфейс.

Почему сделано именно так:

1. Пользователь явно подтвердил, что новый визуальный вариант не подходит.
2. Возврат на старую базу снижает риск скрытых регрессий и ускоряет проверку правок по продукту.

### 2) Автоотклики: без отдельного второго trial-блока

Что работает сейчас:

1. На `autoapply.html` сохранен единый тарифный сценарий (`500 ₽ / месяц`, `до 200 откликов в день`).
2. Калькулятор откликов удален из страницы и из пользовательского сценария.
3. Trial не вынесен в отдельный второй блок: пробный день описан внутри текущей тарифной логики и FAQ.
4. В FAQ убран старый лимит `50 откликов/сутки`, чтобы не конфликтовал с текущей моделью.

Почему сделано именно так:

1. По бизнес-требованию trial должен быть частью основного оффера, а не отдельным лендинг-блоком.
2. Единая тарифная рамка уменьшает путаницу в коммуникации между страницей и ботом.

### 3) Разовые услуги по 5 000 ₽

Что работает сейчас:

1. В `interview-prep` есть два разовых формата по `5 000 ₽`:
   - `1 mock-собеседование`;
   - `Составление резюме`.
2. В `grade-salary` также зафиксированы два разовых формата по `5 000 ₽`:
   - `1 mock-собеседование`;
   - `Составление резюме` (вместо прежнего аудита за `8 900 ₽`).

Почему сделано именно так:

1. Это прямое требование продукта: два понятных one-time формата с одинаковым ценником.
2. Обновление выполнено в data-layer (`config.js`), поэтому рендер карточек остается единым и предсказуемым.

### 4) Контакты снизу сайта

Что работает сейчас:

1. В общий footer на всех страницах добавляется единый контактный блок:
   - ментор `@LeonovCare`;
   - бот `@Leonov_Care_bot`;
   - канал `t.me/olegleonoff`;
   - группа `t.me/olegleonoff`;
   - YouTube `@leonov_care`.
2. В `offer.html` контакты также явно перечислены в юридическом блоке.

Почему сделано именно так:

1. Футер формируется централизованно через `script.js`, чтобы не править десятки HTML-страниц вручную.
2. Дублирование контактов в оферте сохраняет юридическую прозрачность и независимость от JS-рендера.

### 5) Блок зарплат по направлениям (фото 2)

Что работает сейчас:

1. В `languageSalaryChart.data` расширен список направлений:
   - Go / Golang;
   - Java;
   - DevOps / SRE;
   - Frontend (JS/TS);
   - Backend;
   - Python;
   - C# / .NET;
   - Data Engineering;
   - PHP.

Почему сделано именно так:

1. Выполнен запрос «дополнить список на фото 2» именно в том блоке, который рендерит график зарплат на главной.
2. Привязка `track` сохранена, чтобы клик по строке вел на соответствующую страницу направления.

## Актуализация 2026-04-18 (главная страница: удаление промо-блока Interview Helper)

Что работает сейчас:

1. На главной странице удален отдельный промо-блок `Interview Helper` (секция `#interview-helper-launch`).
2. Остальной контент главной страницы (офферы, процесс, FAQ, отзывы, CTA) остается без изменений.
3. Страница `interview-helper.html` как отдельная посадочная продолжает работать.

Почему сделано именно так:

1. По прямому запросу владельца сайта блок не должен отображаться на главной.
2. Информация о продукте уже раскрывается в карточках/профильных разделах, поэтому отдельное повторение убрано для более чистой структуры страницы.

## Актуализация 2026-04-18 (график зарплат: убран переключатель + расширены направления)

Что работает сейчас:

1. На страницах `directions.html` и `grade-salary.html` удален переключатель сортировки графика (`Лидеры` / `От меньшей`).
2. График по умолчанию показывает список в порядке убывания дохода (лидирующие направления сверху).
3. В `languageSalaryChart.data` добавлены ролевые направления помимо стековых:
   - Product Manager;
   - SRE;
   - Data Science (ML);
   - AQA (Automation);
   - System Analyst;
   - Security Engineer;
   - QA (Manual + General);
   - Business Analyst;
   - Project Manager;
   - Data Analyst.
4. Для части существующих направлений обновлены ориентиры по данным калькулятора Хабр Карьеры (например Backend, DevOps, Data Engineering).

Почему сделано именно так:

1. Пользователь попросил убрать сам переключатель и оставить более простой сценарий чтения графика.
2. Пользователь отдельно запросил попробовать найти зарплатные данные и по другим направлениям, поэтому добавлены подтвержденные ролевые данные.
3. Источник в `config.js` переведен на зарплатный калькулятор Хабр Карьеры (`career.habr.com/salaries`) с сохранением связи с обзором за 2 полугодие 2025 в примечании.

## Актуализация 2026-04-18 (финальная зачистка переключателя + доп. направления)

Что работает сейчас:

1. Из `script.js` удалена остаточная JS-логика переключателя сортировки зарплатного графика (`data-chart-sort`, `sortMode`, обработчики кнопок).
2. В `styles.css` удалены неиспользуемые стили визуального переключателя (`.chart-controls`, `.chip-btn`, `.chip-btn.is-active`), чтобы код соответствовал фактическому интерфейсу.
3. В `languageSalaryChart.data` добавлены ещё проверенные направления из калькулятора Хабр Карьеры:
   - `Product Analyst` — `235000`;
   - `Web Analyst` — `169750`.
4. Новые направления привязаны к существующему треку `analytics`, чтобы сохранялась рабочая навигация при клике по строке графика.

Почему сделано именно так:

1. Удаление только HTML-кнопки недостаточно: оставшаяся JS/CSS-логика создает технический долг и риск расхождений при дальнейшем рефакторинге.
2. Дополнительные направления добавлены только после валидации по `career.habr.com/salaries`, чтобы не подтянуть «дефолтные» значения без подтвержденной специализации.
3. Привязка к уже существующему треку `analytics` снижает риск 404/битых переходов и сохраняет целостный пользовательский сценарий.

## Актуализация 2026-04-18 (Mock + резюме: бесплатно для учеников и отдельная платная карточка)

Что работает сейчас:

1. В карточке услуги `После курсов до оффера` больше не показывается формулировка, что mock и резюме всегда платные.
2. В этой же карточке явно зафиксировано текущее правило: для учеников LeonovCare `mock-собеседования` и `составление резюме` включены бесплатно в сопровождение.
3. В блок услуг добавлена отдельная сервисная карточка:
   - `Mock + резюме (разово)`;
   - цена `5 000 ₽ за услугу`;
   - переход на `interview-prep.html#service-tariffs`.
4. В тарифах страниц `interview-prep` и `grade-salary` разовые планы (`1 mock-собеседование`, `Составление резюме`) отмечены как:
   - `5 000 ₽ (отдельно)` для точечного подключения;
   - `бесплатно для учеников LeonovCare` в рамках сопровождения.
5. В заметках тарифов (`serviceTariffs.note`) для обеих страниц добавлено пояснение про бесплатность этих разовых услуг для учеников.

Почему сделано именно так:

1. Пользовательский запрос требует одновременно двух режимов: бесплатное включение для учеников и отдельный платный вход для тех, кто подключает только разовую услугу.
2. Разделение на две сущности (включено в сопровождение + отдельная платная карточка) убирает конфликт в коммуникации и снижает риск неверного ожидания по цене.
3. Привязка отдельной карточки к `interview-prep.html#service-tariffs` дает быстрый переход сразу к месту оформления разовой услуги, не ломая структуру существующих страниц.

## Актуализация 2026-04-18 (выравнивание кнопок в карточках услуг)

Что работает сейчас:

1. В карточках блока услуг (`service-card`) кнопки теперь стоят на одной нижней линии внутри каждой строки сетки.
2. Карточка построена как колонка (`display: flex; flex-direction: column`), список преимуществ занимает гибкое пространство, а кнопка прижимается вниз.
3. Даже при разной длине текстов и списков интерфейс не «скачет» по вертикали между соседними карточками.

Почему сделано именно так:

1. Ранее кнопка шла сразу после контента, поэтому карточки с коротким списком показывали кнопку выше, чем карточки с длинным списком.
2. Фикс сделан в CSS-слое (`styles.css`) без изменения данных и HTML-шаблонов, что безопаснее для существующей логики рендера.
3. Такой подход уже используется в `pricing-card`, поэтому поведение карточек становится единообразным по всему сайту.

## Актуализация 2026-04-18 (устранение дубля в блоке автооткликов)

Что работает сейчас:

1. В секции `Прозрачные цены на отклики` на `autoapply.html` убрано смысловое дублирование между карточками.
2. Левая карточка `Тариф` показывает только основной тариф:
   - `500 ₽ / месяц: до 200 откликов в день`.
3. Правая карточка переименована в `Пробный период` и показывает только trial-условие:
   - `Бесплатно (1 день)`;
   - доступ к Interview Helper и автооткликам в рамках текущего лимита.
4. В `script.js` удален неиспользуемый остаток рендера `autoapply-packs`, чтобы убрать лишнюю ветку логики и поддерживать только актуальные блоки (`rates` + `subscriptions`).

Почему сделано именно так:

1. Пользователь видел два похожих блока (`Тариф` и `Подписка`) с почти одинаковым содержанием, что воспринималось как дубль и снижало доверие к интерфейсу.
2. Разделение на `основной тариф` и `пробный период` делает блок понятным с первого просмотра и убирает повтор цены.
3. Чистка мертвого кода в `renderAutoapplyPricing` уменьшает риск повторного появления дублей при следующих правках.

## Актуализация 2026-04-18 (исправление открытия полного текста отзывов + стиль кнопки)

Что работает сейчас:

1. Кнопка `Читать полностью` в карточках API-отзывов теперь визуально оформлена в едином стиле сайта (цвет, границы, hover, focus).
2. Открытие полного текста отзывов происходит в отдельном модальном окне `review-modal` с затемнением фона, корректным z-index и закрытием:
   - по кнопке `×`,
   - по клику на фон,
   - по `Esc`.
3. В карточках отзывов добавлены недостающие стили для структуры:
   - `review-api-body`,
   - `review-api-date`,
   - корректное нижнее выравнивание кнопки `review-expand-btn`.
4. В модальном окне оформлены:
   - шапка с автором и датой,
   - читаемый текст с нормальным переносом,
   - адаптивные размеры под экран.

Почему сделано именно так:

1. Проблема была в отсутствии CSS-правил для `review-expand-btn` и `review-modal`: кнопка выглядела как дефолтный системный элемент, а открытие «полного отзыва» визуально не воспринималось как полноценный UI.
2. Логика открытия уже была в `script.js`, поэтому исправление выполнено через доопределение стилей без ломки существующего JS-потока загрузки отзывов.
3. Добавлены состояния `hover/focus` и сценарии закрытия модалки, чтобы блок отзывов был не только рабочим, но и аккуратным в UX.

## Актуализация 2026-04-18 (исправление больших отступов в bullets карточек услуг)

Что работает сейчас:

1. В карточках услуг (`service-card`) список `pricing-list` больше не растягивает строки по высоте карточки.
2. Пункты списка отображаются плотным, естественным списком без огромных вертикальных промежутков.
3. Выравнивание кнопки по нижней линии карточки сохранено.

Почему сделано именно так:

1. Растяжение появилось из-за комбинации `flex: 1 1 auto` у списка и сеточной раскладки списка (`display: grid`), из-за чего треки визуально «разъезжались».
2. Добавлено `align-content: start` для `.service-card .pricing-list`, чтобы при свободной высоте карточки строки списка оставались прижаты кверху и не растягивались.
3. Фикс точечный и безопасный: затрагивает только списки в `service-card`, не ломая карточки тарифов и другие блоки.

## Актуализация 2026-04-22 (синхронизация по прод-серверу + полный бэкап проекта и БД)

Что работает сейчас:

1. Локальный репозиторий и Git выровнены по продовому состоянию сервера из `/main/LeonovCare/LeonovCare`.
2. Выполнен полный резерв проекта и БД, и резервные файлы добавлены в Git в каталог `backups/production/20260422_231005/`.
3. Целостность локально скачанных архивов проверена по `SHA256SUMS.txt`.
4. Подробный технический отчёт со всеми шагами и причинами решений зафиксирован в `PROD_SYNC_AND_BACKUP_2026_04_22.md`.

Почему сделано именно так:

1. В рамках текущей задачи прод на сервере является эталоном, поэтому синхронизация выполнена именно в его сторону.
2. Одновременное хранение архивов проекта, git bundle и дампов БД снижает риск потери данных при разных сценариях аварии.
3. Подробная документация нужна для прозрачного ревью и для быстрого восстановления без догадок по процедуре.

## Актуализация 2026-04-22 (единая тарифная сетка: сайт + бот + оплата)

Что работает сейчас:

1. Для `Interview Helper` на сайте и в боте используется единая сетка:
   - пробный день: `1 день бесплатно`;
   - неделя: `500 ₽`;
   - месяц: `1000 ₽`.
2. Для `Автооткликов` на сайте и в боте используется та же единая сетка:
   - пробный день: `1 день бесплатно`;
   - неделя: `500 ₽`;
   - месяц: `1000 ₽`.
3. На лендингах `autoapply.html`, `interview-helper.html`, `directions.html` и в data-layer `config.js` синхронизированы:
   - мета-описания;
   - hero-блоки;
   - FAQ/тарифные формулировки;
   - CTA-кнопки в сторону Telegram-бота.
4. В боте тариф проходит через все ключевые точки касания:
   - выбор тарифа в UI;
   - создание checkout в Cardlink (для платных тарифов);
   - проверка оплаты и выдача результата;
   - payload/metadata в заявках и диагностике платежей.
5. Пробный день для обоих сервисов одноразовый на пользователя и фиксируется отдельными маркерами в настройках БД.

Почему сделано именно так:

1. Единая тарифная сетка снижает риск конфликтов между сайтом, ботом и платежной логикой.
2. Разделение на `trial / week / month` дает короткий бесплатный вход и два понятных коммерческих горизонта без калькуляторов.
3. Явная передача `plan_key / plan_title / duration_days / amount` в платежных метаданных нужна для прозрачной модерации и последующей аналитики.
4. One-time trial защищает от повторного бесплатного использования и делает воронку предсказуемой для финмодели.

## Актуализация 2026-05-02 (замена публичной оферты на договор в PDF)

Этот блок добавлен без удаления старых упоминаний публичной оферты выше: предыдущие записи остаются историческим контекстом, а ниже описано текущее рабочее состояние сайта.

Что работает сейчас:

1. В корень сайта добавлен файл `contract.pdf`. Это актуальный PDF-договор, полученный из исходного файла `Шаблон обучение 1 (1) (3).pdf`.
2. Страница `offer.html` больше не показывает текст публичной оферты. Сейчас она работает как страница `Договор` и содержит:
   - заголовок `Договор`;
   - кнопку открытия PDF в отдельной вкладке;
   - кнопку скачивания договора;
   - встроенный просмотр `contract.pdf` через `iframe`.
3. Во всех HTML-футерах видимый текст ссылки заменен с `Публичная оферта` на `Договор`.
4. URL `offer.html` сохранен, хотя смысл страницы изменился с публичной оферты на договор.

Почему сделано именно так:

1. URL `offer.html` уже присутствует во всех футерах, sitemap и возможных внешних ссылках. Сохранение адреса снижает риск битых ссылок после замены документа.
2. PDF вынесен в отдельный `contract.pdf`, чтобы договор можно было открыть напрямую, скачать и встроить на страницу без ручного переноса юридического текста в HTML.
3. Видимый текст `Договор` убирает конфликт с прежним названием `Публичная оферта` и соответствует текущему документу.
4. Отдельная CSS-обвязка для PDF (`contract-card`, `contract-actions`, `contract-frame`) нужна, чтобы страница осталась читаемой на десктопе и мобильных экранах без изменения общего шаблона сайта.

## Актуализация 2026-05-08 (восстановление работоспособности API-отзывов без удаления исторических блоков)

Этот раздел добавлен поверх предыдущих пунктов. Старые описания выше сохранены как исторический контекст, а ниже зафиксировано текущее рабочее поведение и причины выбранной реализации.

Что работает сейчас:

1. В `config.js` для блока отзывов используется актуальный базовый URL `https://api.2r.ru/v1`.
2. Параметр `perPage` теперь равен `20` (вместо `0`), чтобы запрос не зависал на стороне внешнего API.
3. Добавлены явные параметры устойчивости:
   - `requestTimeoutMs: 12000`;
   - `retryCount: 2`.
4. Функция `renderReviewsFromApi` в `script.js` теперь:
   - делает загрузку с таймаутом через `AbortController`;
   - выполняет повторные попытки загрузки при временных ошибках;
   - поддерживает оба формата ответа (`result.items` и `result.reviews`);
   - сохраняет успешный результат в `localStorage` (`lc_reviews_cache_v2_<telegramId>`);
   - при недоступности API показывает сохраненные отзывы из кэша вместо пустого блока.
5. Если кэш недоступен или пуст, сохраняется явный fallback-статус: `Временно не удалось загрузить отзывы. Попробуйте позже.`

Почему сделано именно так:

1. Внешний API перешел на домен `api.2r.ru`, а старая точка `api.it-mentors.ru` отдает редирект; это увеличивает риск сбоев и задержек при нестабильной сети.
2. Значение `perPage: 0` перестало быть безопасным для текущей реализации внешнего API: блок отзывов периодически уходил в timeout и показывал ошибку пользователю.
3. Ретраи и ограниченный таймаут снижают вероятность ложного падения UI при кратковременных сетевых проблемах.
4. Кэш последних успешных отзывов убирает сценарий «пустой секции» в проде и сохраняет социальное доказательство даже во время инцидента на стороне внешнего сервиса.

## Актуализация 2026-05-13 (снижение цены разовых mock и резюме)

Этот блок добавлен поверх предыдущих записей без удаления истории. Ниже зафиксировано текущее рабочее состояние и почему правка сделана именно так.

Что работает сейчас:

1. Разовая цена услуг `1 mock-собеседование` и `Составление резюме` на сайте установлена в `5 000 ₽`.
2. Цена синхронизирована в карточке `Mock + резюме (разово)` на главной витрине услуг (`servicePillars`).
3. Цена синхронизирована в разовых тарифах разделов:
   - `interview-prep`;
   - `grade-salary`.
4. Текущая цена `5 000 ₽` отражена в технической документации проекта:
   - `README.md`;
   - `SITE_REPRO_SPEC.md`.

Почему сделано именно так:

1. У цены разовых услуг один источник правды в `config.js`, поэтому изменение в data-layer сразу обновляет рендер всех страниц без ручных правок каждого HTML-шаблона.
2. Одновременная правка витринной карточки и тарифных карточек исключает расхождение в коммуникации и убирает риск пользовательской ошибки при выборе услуги.
3. Фиксация цены в документации нужна, чтобы при следующем ревью и переносе проекта не возникало неоднозначности по актуальному прайсу.

## Актуализация 2026-05-15 (перевод кнопок личного контакта ментора на новый Telegram)

Этот блок добавлен append-only поверх предыдущих записей: старые блоки остаются историческим контекстом, а ниже зафиксировано текущее рабочее состояние после изменения контакта.

Что работает сейчас:

1. Все кнопки сайта с `data-link="mentor"` ведут в личный Telegram-чат ментора `@mr_winchester1` (`https://t.me/mr_winchester1`).
2. Переадресация выполнена через единый источник правды `mentorUrl` в `config.js`, поэтому изменение автоматически применяется ко всем страницам и всем mentor-CTA.
3. Ссылки Telegram-бота и остальных Telegram-контактов не изменялись:
   - `botUrl = https://t.me/Leonov_Care_bot`;
   - `channelUrl = https://t.me/olegleonoff`;
   - `groupUrl = https://t.me/olegleonoff`.
4. Подпись контакта ментора в футере синхронизирована с новым личным username (`@mr_winchester1`), чтобы текст и фактический URL не расходились.

Почему сделано именно так:

1. В проекте уже заложена архитектура с разделением типов ссылок (`mentor` и `bot`), поэтому безопаснее и быстрее менять только `mentorUrl`, а не править десятки HTML-файлов вручную.
2. Ограничение изменения только личным контактом снижает риск регрессии в продуктовых воронках, где нужны отдельные маршруты в Telegram-бота.
3. Синхронизация подписи в футере нужна для прозрачности: пользователь должен видеть тот же username, на который реально открывается ссылка.

## Append-only update 2026-05-21 — production variant without public prices

Что работает сейчас:

1. На публичных страницах сайта убраны цены и ценовые формулировки в пользовательских блоках.
2. Карточки сервисов и связанные CTA показывают формат запуска, а не прайс-сценарий.
3. Для клиентского рендера включен флаг `hidePricesCompletely: true` в `config.js`.
4. Добавлена детальная документация релиза и rollback:
   - `SITE_NO_PRICES_PROD_ROLLOUT_2026_05_21.md`.

Почему сделано именно так:

1. Прямое требование релиза: сайт должен выглядеть так, как будто цен в интерфейсе никогда не было.
2. Простое скрытие сумм (типа `Цена по запросу`) оставляет визуальные следы старой pricing-логики, поэтому убрана и риторика.
3. Подход через флаг и изолированный релиз позволяет быстро вернуть предыдущую версию через rollback.

## Актуализация 2026-06-10 (смена Telegram-контакта ментора на `@leonovcare`)

Этот блок добавлен append-only без удаления предыдущих записей. Более ранние секции остаются историческим контекстом, а ниже зафиксировано текущее рабочее состояние после точечной смены личного Telegram-контакта.

Что работает сейчас:

1. Единый `mentorUrl` в `config.js` установлен в `https://t.me/leonovcare`.
2. Все ссылки сайта с `data-link="mentor"` продолжают собираться через `mentorLink` в `script.js`, но теперь открывают личный Telegram ментора `@leonovcare`.
3. Подпись контакта ментора в footer-инжекте синхронизирована с фактической ссылкой и отображается как `@leonovcare`.
4. Остальные публичные контакты не менялись:
   - `botUrl = https://t.me/Leonov_Care_bot`;
   - `channelUrl = https://t.me/olegleonoff`;
   - `groupUrl = https://t.me/olegleonoff`;
   - `youtubeUrl = https://www.youtube.com/@leonov_care`.

Почему сделано именно так:

1. В проекте уже есть один источник правды для личного контакта ментора (`mentorUrl`), поэтому правка в этом месте автоматически покрывает все mentor-CTA без ручного прохода по множеству HTML-страниц.
2. Footer-контакты рендерятся через общий JS-инжект, поэтому подпись нужно синхронизировать отдельно, иначе пользователь увидит старый username при новом `href`.
3. Остальные Telegram-ссылки оставлены без изменений намеренно, чтобы не затронуть воронки бота, канала и группы, не относящиеся к задаче пользователя.
4. Username записан в нижнем регистре ровно в том виде, в котором его передали для бизнеса и ревью, даже несмотря на case-insensitive природу Telegram-ссылок.

## Актуализация 2026-06-26 (резервный показ отзывов при пустом API)

Этот блок добавлен без удаления исторического описания выше. Старые пункты оставлены как контекст, а ниже зафиксировано, как блок отзывов работает сейчас после восстановления.

Что работает сейчас:

1. Основной источник отзывов по-прежнему внешний API `https://api.2r.ru/v1/integrations/mentors/reviews/{telegramId}`.
2. Если API возвращает опубликованные текстовые отзывы, сайт рендерит их в `#reviews-api-track`, сохраняет успешный результат в `localStorage` и показывает текущий API-контент.
3. Если API временно недоступен, сначала используется браузерный кэш `lc_reviews_cache_v2_<telegramId>`.
4. Если API отвечает пустым списком или у нового посетителя еще нет кэша, сайт показывает резервные скриншоты из `config.reviews.fallbackItems`.
5. Резервные карточки используют существующие файлы `Feedback/photo_*_2026-03-06_14-57-59.jpg`, карусель отзывов и lightbox для просмотра скрина крупно.
6. На главную возвращена разметка `#lightbox`, потому динамический fallback со скриншотами должен открываться так же, как в первой прод-версии сайта.

Почему сделано именно так:

1. Инцидент 26.06.2026 подтвердил, что API может вернуть `200 OK` с `items: []`; это не сетевой сбой, поэтому retry и `localStorage` не помогают новым посетителям.
2. Резерв из репозитория надежнее, чем ручная вставка HTML: список скриншотов хранится рядом с API-настройками в `config.js`, а рендер остается централизованным в `script.js`.
3. Используются уже существующие реальные скриншоты отзывов, поэтому блок не превращается в искусственные плейсхолдеры и продолжает выполнять функцию социального доказательства.
4. API остается приоритетным источником, чтобы после восстановления внешней выдачи сайт автоматически вернулся к актуальным отзывам без нового релиза.
